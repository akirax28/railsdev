# SQLite. Versions 3.8.0 and up are supported.
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#
default: &default
  adapter: postgresql
  encoding: utf8
  pool: 5
  username: postgres
  password: enter_password
  host: db 


development:
  <<: *default
  database: project_dev

test:
  <<: *default
  database: project_teste

production:
  <<: *default
  database: project_prod
